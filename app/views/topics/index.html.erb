<h1>Listing topics</h1>

<table>
  <thead>
    <tr>
      <th align=left>Topic</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @topics.each do |topic| %>
      <tr>
        <td><%= link_to topic.title, topic %></td>
        <td><%= pluralize(topic.votes.count, "vote") %></td>
        <td><%= button_to '+1', upvote_topic_path(topic), method: :post %></td>
          <td><%=
          if topic.votes.count > 0
            button_to '-1', downvote_topic_path(topic), method: :post
          else '-1'
          end %></td>
        <td><%= link_to 'Delete', topic, method: :delete, data: { confirm: 'Kill it?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Topic', new_topic_path %>

<br><br>
Totally unrelated image of a kitty:<br>
<%= image_tag('http://placekitten.com/200/200') %>
